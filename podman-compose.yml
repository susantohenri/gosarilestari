services:
  app:
    build:
      context: .
    container_name: gosarilestari-app
    ports:
      - "8080:80"
    volumes:
      - .:/var/www/html:z
    depends_on:
      - db

  db:
    image: mysql:8
    container_name: gosarilestari-db
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: gosarilestaridb
    ports:
      - "3306:3306"
    volumes:
      - mysql_data:/var/lib/mysql

volumes:
  mysql_data: